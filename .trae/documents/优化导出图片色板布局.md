我已分析了负责导出图片和绘制色板的代码 `src/utils/imageDownloader.ts`。为了响应您的优化需求，我制定了以下修改计划，旨在减小底部色板区域的空间占用，使其更紧凑。

### 计划修改内容

1.  **调整色板布局算法 (src/utils/imageDownloader.ts)**
    *   **增加每行色块数量**：将计算列数的基准宽度从 `100px` 调整为 `70px`。这将使每行显示更多色块，从而自动减小每个色块的尺寸。
    *   **减小色块高度比例**：将色块高度从宽度的 `65%` 调整为 `55%`，使其更扁平。
    *   **减小间距**：
        *   `cardGap`（色块间距）：从 `10px` 减小到 `6px`。
        *   `statsTopMargin`（色板顶部边距）：从 `24px` 减小到 `15px`。
        *   `statsPadding`（色板左右边距）：从 `20px` 减小到 `15px`。
    *   **压缩标题和页脚**：
        *   `titleHeight`（色板区域标题高度）：从 `30px`/`40px` 减小到 `20px`。
        *   `footerHeight`（页脚总计高度）：从 `50px` 减小到 `30px`。

2.  **优化文字显示**
    *   调整色号和数量的字体大小计算逻辑，确保在更小的色块中依然清晰可辨（设定合理的最小字号）。

### 预期效果
*   底部色板区域的高度将显著减小。
*   色块排列更加紧凑，视觉上不再喧宾夺主。
*   导出的图片整体更加协调，突显上方的图纸主体。
